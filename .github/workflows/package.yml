name: Build and test KittenTTS wheel

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/package.yml"
      - "kittentts/**"
      - "tests/**"
      - "README.md"
      - "docs/native_engine_release.md"
      - "MANIFEST.in"
      - "pyproject.toml"
      - "setup.py"
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  unit-tests:
    name: Unit tests Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.8"
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package without native runtime deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e . --no-deps

      - name: Run tests
        run: python -m unittest discover -s tests

  build:
    name: Build py3-none-any wheel and sdist
    needs: unit-tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Build distribution
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
          python -m build
          python -m twine check dist/*

      - name: Verify wheel tag
        run: |
          python - <<'PY'
          from pathlib import Path

          wheels = sorted(Path("dist").glob("kittentts-*.whl"))
          assert len(wheels) == 1, wheels
          wheel = wheels[0]
          print(wheel)
          assert wheel.name.endswith("-py3-none-any.whl"), wheel.name
          PY

      - uses: actions/upload-artifact@v6
        with:
          name: kittentts-dist
          path: dist/*

  installed-wheel-smoke:
    name: ${{ matrix.platform }} Python ${{ matrix.python-version }} installed wheel
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: Linux x86_64
            os: ubuntu-latest
            python-version: "3.8"
          - platform: Linux x86_64
            os: ubuntu-latest
            python-version: "3.9"
          - platform: Linux x86_64
            os: ubuntu-latest
            python-version: "3.10"
          - platform: Linux x86_64
            os: ubuntu-latest
            python-version: "3.11"
          - platform: Linux x86_64
            os: ubuntu-latest
            python-version: "3.12"
          - platform: Linux x86_64
            os: ubuntu-latest
            python-version: "3.13"
          - platform: Linux x86_64
            os: ubuntu-latest
            python-version: "3.14"
          - platform: Linux ARM64
            os: ubuntu-24.04-arm
            python-version: "3.8"
          - platform: Linux ARM64
            os: ubuntu-24.04-arm
            python-version: "3.11"
          - platform: Linux ARM64
            os: ubuntu-24.04-arm
            python-version: "3.14"
          - platform: Windows x86_64
            os: windows-2025-vs2026
            python-version: "3.8"
          - platform: Windows x86_64
            os: windows-2025-vs2026
            python-version: "3.11"
          - platform: Windows x86_64
            os: windows-2025-vs2026
            python-version: "3.14"
          - platform: Windows ARM64
            os: windows-11-vs2026-arm
            python-version: "3.9"
          - platform: Windows ARM64
            os: windows-11-vs2026-arm
            python-version: "3.11"
          - platform: Windows ARM64
            os: windows-11-vs2026-arm
            python-version: "3.14"
          - platform: macOS ARM64
            os: macos-26
            python-version: "3.8"
          - platform: macOS ARM64
            os: macos-26
            python-version: "3.11"
          - platform: macOS ARM64
            os: macos-26
            python-version: "3.14"
          - platform: macOS x86_64
            os: macos-15-intel
            python-version: "3.8"
          - platform: macOS x86_64
            os: macos-15-intel
            python-version: "3.11"
          - platform: macOS x86_64
            os: macos-15-intel
            python-version: "3.14"

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - uses: actions/download-artifact@v7
        with:
          name: kittentts-dist
          path: dist

      - name: Install built KittenTTS wheel with PyPI native deps
        run: |
          python -m pip install --upgrade pip
          python -c "from pathlib import Path; import subprocess, sys; wheel = next(Path('dist').glob('kittentts-*-py3-none-any.whl')); subprocess.check_call([sys.executable, '-m', 'pip', 'install', '--only-binary=kitten-inference', str(wheel)])"

      - name: Smoke test installed wheel
        run: |
          python -c "import importlib.metadata as md; import kittentts; import model_inference as mi; print('kittentts', md.version('kittentts')); print('kitten-inference', md.version('kitten-inference')); print(mi.__file__); assert mi.set_backend('cpu'); assert mi.active_backend() == 'cpu'; assert kittentts.normalize_text('Fig. 2') == 'Figure two'"

      - name: Run tests against installed wheel
        run: python -m unittest discover -s tests

  audio-smoke:
    name: Generate release smoke audio
    needs: build
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - uses: actions/download-artifact@v7
        with:
          name: kittentts-dist
          path: dist

      - name: Install built KittenTTS wheel with PyPI native deps
        run: |
          python -m pip install --upgrade pip
          python -c "from pathlib import Path; import subprocess, sys; wheel = next(Path('dist').glob('kittentts-*-py3-none-any.whl')); subprocess.check_call([sys.executable, '-m', 'pip', 'install', '--only-binary=kitten-inference', str(wheel)])"

      - name: Generate audio samples
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_HUB_DISABLE_TELEMETRY: "1"
        run: python tests/audio_smoke.py --out-dir audio-smoke --cache-dir .hf-cache

      - uses: actions/upload-artifact@v6
        with:
          name: kittentts-audio-smoke
          path: audio-smoke/*
